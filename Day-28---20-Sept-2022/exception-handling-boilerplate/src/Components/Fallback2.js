export default function Fallback2({ error }) {
    return (
        // Display message 'Fallback Page for Page-2` when error raises in Child2 component
        <div>Fallback Page for Page-2</div>
    )
}