export default function Fallback1({ error }) {
    return (
        // Display message 'Fallback Page for Page-1` when error raises in Child1 component
        <div>Fallback Page for Child1</div>
    )
}